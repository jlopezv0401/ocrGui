GUI for ocr project, coded in Visual Basic 2008.

To use the COM component generated by MATLAB, first install the executable package to re register te component on the system, then add the reference on Visual Studio, and now you can instanciate a new object using the ocrMain class, use the paremeters recomended in the "ocr" repository
